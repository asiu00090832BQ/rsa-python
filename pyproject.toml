[project]
name = "rsa_python"
version = "0.1.0"
description = "High-integrity pure-Python RSA implementation for testing"
authors = [{name="Developer"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
[tool.pytest.ini_options]
minversion = "6.0"
